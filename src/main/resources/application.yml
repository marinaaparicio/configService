spring:
  application:
    name: "config-server"
  cloud:
    config:
      server:
        git:
          uri: http://github.com/pruedaesnt/bootCamp-ConfigServer
          default-label: main
          search-paths:
          - '*.yaml'
              
server:
  port: 8888
